<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ns.dao.CourseDao">

	 <select id="getLectureCount"  resultType="int" >
	 		SELECT COUNT(*) CNT 
	 		FROM Lecture
	 		WHERE ${param1} LIKE '%${param2}%'
	 </select>
	 
	 
	   <select id="getLectureList"  resultType="vo.Lecture">
	   				SELECT * 
	   				FROM
  							(
  									SELECT ROWNUM NUM, N.* 
  									FROM (
  														SELECT * 
  														FROM NOTICES 
  														WHERE  ${param2} LIKE '%${param3}%' ORDER BY REGDATE DESC
  											   ) N
  					        )
  					WHERE NUM BETWEEN 1 + (${param1}-1)*5 AND 5 + (${param1}-1)*5
	 	  </select>
	   
	 
<!-- 	      <delete id="deleteLecture">
	      		DELETE NOTICES WHERE SEQ=#{seq}
	      </delete>
	      
	      
	     
	    
	     <update id="updateLecture"  parameterType="vo.Lecture">
	     		UPDATE NOTICES 
	     		SET TITLE=#{title}, 
	     			    CONTENT=#{content}, 
	     			    FILESRC=#{fileSrc , jdbcType=VARCHAR} , 
	     			    FILESRC2=#{fileSrc2 , jdbcType=VARCHAR} 
	     		WHERE SEQ=#{seq}
	     </update>
	     
	     <select id="getLectureDetail"    resultType="vo.Lecture">
	     		SELECT seq,title,writer,content,regdate,hit,filesrc,filesrc2 
	     		FROM NOTICES 
	     		WHERE SEQ=#{seq}
	     </select>
	  
	      <insert id="insertLecture"  parameterType="vo.Lecture"  >
	      		<selectKey order="BEFORE" keyProperty="seq" resultType="String">
	      				 	SELECT MAX(TO_NUMBER(SEQ))+1 FROM NOTICES
	      		</selectKey>
	      		INSERT INTO NOTICES(SEQ, TITLE, CONTENT, WRITER, REGDATE, HIT, FILESRC , FILESRC2) 
	      		VALUES(#{seq} , #{title}, #{content}, #{writer}, SYSDATE, 0, #{fileSrc},#{fileSrc2})
	      </insert> -->
</mapper>